export type ProductData = {
    id: string;
    name: string;
    price: string;
    store_name: string;
    photo: string;
}

export type CartItem = {
  photo: string;
  id: string;
  name: string;
  price: string;
  quantity: string;
}